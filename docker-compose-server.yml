services:
  server:
    image: ghcr.io/nugget/vanitykeygen:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - vkg-data:/vkgdata
    environment:
      - VKG_DB_PATH=/vkgdata/vkg.db
      - VKG_LISTEN_PORT=8080

volumes:
  vkg-data:
